function obterSomaDasNotas(estudante) {
  return estudante.notas.reduce((soma, nota) => soma + nota, 0);
}
     
let estudante = {
    nome: "Evandro Carneiro",
    idade: 16,
    notas: [3, 5, 5, 4, 8]
};

console.log(obterSomaDasNotas(estudante));