const multiplicaNumeros = numeros => {
    return numeros.reduce((total, numero) => total * numero, 1);
}

console.log(multiplicaNumeros([10, 20, 30])); // 6000
console.log(multiplicaNumeros([2, 4, 2, 10])); // 160
