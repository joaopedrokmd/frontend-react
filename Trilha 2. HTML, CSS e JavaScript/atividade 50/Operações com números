<!DOCTYPE html>
<html lang="pt-br">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Doc</title>
    <style>

    </style>
  </head>
  <body>
    <nav class="navbar">
        <h1 class="nav-brand">Resto da Divisão</h1>
    </nav>

    <main class="container">
        <div>
            <label class="label" for="componente-nome">Entre com um número</label>
            <input type="number" class="input" placeholder="Entre com número" id="teu-numero" autocomplete="off">
        </div>

        <p>O resto da divisão por 2 é <span id="output"></span>.</p>
    </main>
    <script>
       
    function obterRestoDeDivisaoPor2(numero) {
          return numero % 2;
}

        
        const numero = document.querySelector("#teu-numero");
        const output = document.querySelector("#output");

        numero.addEventListener("keyup", () => {
            output.textContent = obterRestoDeDivisaoPor2(Number.parseInt(numero.value, 10));
        });
    </script>
  </body>
</html>