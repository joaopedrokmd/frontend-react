const obterTemperaturasPositivas = temperaturas => {
    return temperaturas.filter(temperatura => temperatura > 0);
}

// Exemplo de uso - não modifique
console.log(obterTemperaturasPositivas([-5, 12, 3])); // [12, 3]
console.log(obterTemperaturasPositivas([1, -3, -2, 4, 10])); // [1, 4, 10]
